{
  "name": "ai-pr-review-assistant",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal TypeScript GitHub Action scaffold for an AI PR reviewer.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "devDependencies": {
    "@types/node": "20.16.10",
    "typescript": "5.6.3",
    "vitest": "1.6.0"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  }
}
